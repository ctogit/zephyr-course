description: Our super driver extension

#compatible: "zephyr,our-driver"
compatible: "our,sensor"

include: base.yaml

properties:
  pin-gpios:
    type: phandle-array
    required: true
    description: pin connected
