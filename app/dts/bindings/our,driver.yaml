description: Our super driver

#compatible: "zephyr,our-driver"
compatible: "our,driver"

include: base.yaml

properties:
  pin-gpios:
    type: phandle-array
    required: true
    description: pin connected
