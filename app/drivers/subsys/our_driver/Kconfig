menuconfig OUR_DRIVER
    bool "Enable our driver"
    select GPIO
    depends on DT_HAS_OUR_DRIVER_ENABLED
    help
        Our super dupper driver

if OUR_DRIVER
config OUR_DRIVER_INIT_STATE
    bool "Start as low"
    default n
endif